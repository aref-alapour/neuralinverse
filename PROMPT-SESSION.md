# PROMPT-SESSION.md — پرامپت شروع session توی این پروژه

این متن رو اول هر session جدید توی ZCode (با workspace باز شده روی همین
پوشه) پیست کن:

---

سلام. تو توی پروژه توسعه‌ی NeuralInverse هستی (fork کد VS Code/Void).
ورک‌اسپیس فعلی رو با `AGENTS.local.md` بخوان و از این لحظه تمام قوانینش
برایت الزامی است — مخصوصاً دروازه‌ی تایید: هیچ push/issue/PR بدون تایید
صراحح من. زبان چت فارسی، artifact ها انگلیسی.

قبل از شروع هر کار:
1. `git status` و `git log --oneline -5` بزن و بگو روی چه شاخه‌ای هستیم.
2. وضعیت upstream رو چک کن (`gh pr list --repo NeuralInverse/neuralinverse
   --author aref-alapour` و issue های باز ما) و در یک خط گزارش کن.
3. اگه برنامه‌ی نصب‌شده تازگی آپدیت شده، اول از من بپرس آیا Repatch زدم؛
   تا موقعی که نزده باشم هیچ تست لایوی معتبر نیست.

بعد منتظر تسک من بمان. برای هر تسک طبق «Session workflow» در
AGENTS.local.md کار کن: بررسی → پیاده‌سازی روی سورس → پچ زنده → تست من →
فقط بعد از تایید صریح، شاخه‌ی تمیز + push + issue/PR. اگر جایی مردد بودی
که کاری خارج از قوانین است، اول بپرس بعد دست بزن.

---
