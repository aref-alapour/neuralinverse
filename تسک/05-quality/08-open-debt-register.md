# Q8 — بدهی‌های باز: فهرست واحدِ «مانده و انجام‌نشده»

- **اولویت:** فهرست است، نه کار واحد | **وضعیت:** 🟡 (زنده — با بسته‌شدن هر آیتم به‌روز شود)
- **منبع:** [رجیستر ضعف‌ها ۲۰۲۶-۰۹-۰۶](project-status-and-weaknesses-2026-09-06.md)
- **قاعده:** این فایل **منبع سوم حقیقت نمی‌سازد.** هر آیتمی که فایل تسک دارد فقط
  اینجا ارجاع داده می‌شود؛ فقط آیتم‌های بی‌خانه اینجا تعریف کامل دارند.

## ۰) قاعده‌ی وضعیت — رفع ریشه‌ای R1

دریفت تکراری بین جدول README و فایل‌های تسک به این دلیل بود که وضعیت‌ها «نظر»
بودند. از این پس وضعیت **تابع گیت‌های Definition of Done** است، نه تازگیِ سند:

| نماد | تعریف قطعی |
|---|---|
| 🔴 | G1 (سورس) هنوز شروع نشده یا ناقص است |
| 🟡 | G1+G2 سبز (کد نوشته و کامیت شده) ولی G3 (live-patch/`--verify`) یا G4 (تست مالک) باز است |
| ✅ | تا G4 سبز — یعنی مالک روی نسخه‌ی قابل‌اجرا دیده و تأیید کرده |

**نتیجه‌ی فوری این قاعده:** M1، M2 و M5 هر سه 🟡 اند (کد هست، تست زنده نیست) —
پس فایل‌های M1/M2 باید از 🔴 به 🟡 بروند، نه اینکه README به 🔴 برگردد.

## ۱) آیتم‌هایی که فایل تسک دارند — فقط اولویت عوض می‌شود

| آیتم | فایل | وضعیت | چرا در صف است |
|---|---|---|---|
| **C1** سیم‌کشی موتور context به مسیر پیش‌ازارسال | [C1](../02-context/01-wire-context-engine.md) | 🔴 | زیرساخت هر دو موجود است (موتور + `contextAssembler`)؛ کار اصلی فقط سیم‌کشی pre-send است. کاندیدای طبیعی «تسک بعد از بیلد» |
| **A1** وصل کردن LLM به background agents | [A1](../03-agent-parity/01-background-agents-loop.md) | 🔴 | نسخه‌ی محصولیِ همان چیزی که مالک از خودِ agent می‌خواهد: تسک بده، پس‌زمینه اجرا شود، خودش تست کند، نتیجه برگردد |
| **Q3** یکپارچه‌سازی سه stack (فاز ۰: سند معماری) | [Q3](03-unify-agent-stacks.md) | 🔴 | تا این نباشد هر feature سه‌بار پیاده می‌شود (S2) |
| **M3 / M4** ثبت خودکار حافظه، ایندکس @Docs | [M3](../01-memory/03-auto-capture-consolidation-ui.md) / [M4](../01-memory/04-docs-knowledge-base.md) | 🔴 | روی زیرساخت M5 سوارند؛ بعد از تست زنده‌ی Ledger |
| **C2..C7** بقیه‌ی دسته‌ی context | `02-context/` | 🔴/🟡 | کل دسته دست‌نخورده است — صفر کامیت |

## ۱-الف) به‌روزرسانی ۲۰۲۶-۰۹-۰۸ (بعد از [ممیزی سه‌گانه](task-vs-code-audit-2026-09-08.md))

**آیتم‌هایی که خانه‌دار شدند** — از این رجیستر خارج و به فایل تسک منتقل:

| آیتم | مقصد | چرا |
|---|---|---|
| **N5** (bundle های tracked/ignored) و مسئله‌ی contrib مرده | [Q13](13-contribution-registration-gate.md) | آن‌قدر بزرگ شد که فایل تسک لازم داشت؛ بلاک‌کننده‌ی A1/A6 |
| **N7** (جدول قیمت و مدل ناشناخته) | [Q1](01-cost-usage-tracking.md) + [Q12](12-desktop-usage-parity.md) | ریشه‌اش نرسیدن `usage` بود، نه جدول قیمت |
| **N3** (همگام‌سازی وضعیت‌ها) | ✅ **بسته شد** | قاعده‌ی وضعیت در README درج شد؛ M1/M2/A3/A5/Q1/G6 اصلاح شدند |

**آیتم‌های جدید:**

### N11 — تست برابری قرارداد بین مسیر web و desktop
- **شدت:** 🔴 | **برآورد:** S | **وضعیت:** ✅ بسته شد ۰۹-۰۸ — تست پاریتی نوشته و سبز شد (کامیت `213acb14`؛ تعداد/فیلدهای هر نقطه‌ی onFinalMessage بین دو مسیر + لنگرهای usage؛ در verify.mjs اجرا می‌شود). دریفت‌های غیرقراردادی پیدا شده جداگانه برای Q3 ثبت شدند
- **مسئله:** `sendLLMMessage` دو پیاده‌سازی موازی دارد که دستی هم‌گام نگه داشته
  می‌شوند و هیچ گیتی دریفتشان را نمی‌گیرد. `usage` نمونه‌ی کشف‌شده است؛ فرض
  اینکه تنها مورد باشد بی‌پایه است.
- **معیار پذیرش:** تستی که امضای `onFinalMessage` و `onText` را بین دو فایل
  مقایسه کند و هر فیلدی که یکی می‌فرستد و دیگری نه را قرمز کند.

### N12 — باگ ویندوز در مسیر worktree
- **شدت:** 🟠 (نهفته — سرویس ثبت نشده) | **برآورد:** S | **وضعیت:** 🔴
- **مسئله:** `backgroundAgentService.ts:62` مسیر `` `/tmp/ni-bg-${id}` `` را هاردکد
  کرده. ویندوز پلتفرم اصلی مالک است. دامنه‌اش در [A1](../03-agent-parity/01-background-agents-loop.md).

### N13 — ابزار plan mode بدون enforcement
- **شدت:** 🔴 (ایمنی) | **برآورد:** S | **وضعیت:** 🟡 بسته شد ۰۹-۰۸ (قدم اول A5) — گارد در مرز `callTool`، کامیت `feffd9a3` + ۸ تست؛ پورت live-patch و تست owner در [A5](../03-agent-parity/05-plan-mode.md) باز است
- **مسئله:** `getThreadPlanMode` صفر caller دارد. کاربر Plan را روشن می‌کند و هیچ
  write ای بلاک نمی‌شود. دامنه‌اش در [A5](../03-agent-parity/05-plan-mode.md) بند اول.

## ۲) آیتم‌های بی‌خانه — تعریف کامل اینجاست

### N1 — slice برای type-check سورس react (بستن V2)
- **شدت:** 🟠 | **برآورد:** S | **وضعیت:** 🔴
- **مسئله:** `react/src/*.tsx` در هیچ slice ای نیست و tsup/esbuild فقط transpile
  می‌کند؛ یعنی تغییرات UI (ازجمله فوتر) هرگز type-check نشده‌اند.
- **معیار پذیرش:** یک `tsc --noEmit` مستقل (یا slice دوم در `typecheck-slice.json`)
  که مسیرهای `react/src` را پوشش می‌دهد و در `tools/verify.mjs` به‌عنوان گیت
  مستقل گزارش می‌شود؛ گزارش پوشش باید عدد react را جدا نشان دهد.

### N2 — فایل تسک گذشته‌نگر برای فوتر پیام (بستن R4)
- **شدت:** 🟡 | **برآورد:** S | **وضعیت:** 🔴
- **مسئله:** یک feature کامل (`aca402a` + ۵ پچ باندل) بدون فایل تسک، بدون معیار
  پذیرش و بدون ردیف در README وارد سورس و باندل شد.
- **معیار پذیرش:** فایل تسک با دامنه، معیار پذیرش، وضعیت بر اساس قاعده‌ی بخش ۰
  (الان 🟡 — G4 باز)، و ثبت W3 (پیام‌های قدیمی فوتر ندارند) و D5 (پیام‌های abort
  شده در نسخه‌ی نصبی زمان ندارند) به‌عنوان رفتار پذیرفته‌شده.

### N3 — همگام‌سازی وضعیت‌ها + درج قاعده در README (بستن R1)
- **شدت:** 🟠 | **برآورد:** S | **وضعیت:** 🔴
- **معیار پذیرش:** (الف) جدول تعریف وضعیت بخش ۰ بالای جدول README؛ (ب) M1 و M2
  در فایل خودشان 🟡 با شاهد کامیت (`journal` برای M1، `8458f3a` برای M2)؛
  (ج) پاورقی «M5 در دسته‌ی memory است با وجود اسم context».

### N4 — سیاست گزارش تست‌های LFS-exempt (پیش‌نیاز فاز ۴ از Q7)
- **شدت:** 🟠 | **برآورد:** S | **وضعیت:** 🔴
- **مسئله:** ۹۷ فایل LFS ریپو (همه fixture تست کوپایلت) به‌خاطر تمام‌شدن بودجه‌ی
  LFS به‌صورت pointer متنی روی دیسک‌اند و تست‌های simulation را می‌شکنند.
- **معیار پذیرش:** `test-node` دو عدد گزارش کند — fail خام، و fail واقعی = خام
  منهای مواردی که **با نام fixture اثبات شده** ریشه‌شان pointer بودن است. عدد
  دوم عددِ رسمی پروژه است.

### N5 — تناقض ignore و track در `react/out` (بستن R3)
- **شدت:** 🟡 | **برآورد:** S | **وضعیت:** 🟡 ۰۹-۰۸ — تحلیل کامل + پیشنهاد (گزینه A: استثنای صریح `!react/out/**` در .gitignore و اصلاح `react/.gitignore`) در [Q2](02-codebase-hygiene.md)؛ تصمیم نهایی با مالک
- **مسئله:** ۹ فایل `react/out/*/index.js` تِرَک شده‌اند ولی `.gitignore:33` و
  `react/.gitignore:1` مسیرشان را ignore می‌کنند؛ بعد از کامپایل بعدی هر باندل
  **جدید** بی‌صدا از `git add` جا می‌ماند.
- **معیار پذیرش:** یا استثنای صریح (`!react/out/**`) اضافه شود، یا آرتیفکت‌ها از
  ریپو خارج و به مرحله‌ی بیلد سپرده شوند — تصمیمش با مالک؛ ولی حالت فعلی نماند.

### N6 — عدد هاردکد `N_PATCHES - 6` در selftest (بستن P3)
- **شدت:** 🟡 | **برآورد:** S | **وضعیت:** 🔴
- **معیار پذیرش:** شمارش chained exception ها از خود ساختار `PATCHES` مشتق شود،
  نه با تفریق ثابت؛ افزودن یک پچ chained جدید نباید selftest را بشکند.

### N7 — جدول قیمت مدل‌ها (بستن X1)
- **شدت:** 🟡 | **برآورد:** S/M | **وضعیت:** 🔴
- **مسئله:** `budgetTracker.ts:33` فقط ۸ مدل دارد (`gpt-4-turbo`, `gemini-1.5-*`, …)
  در برابر ادعای ۲۲ provider؛ برای هر مدل بیرون این لیست برآورد هزینه بی‌معنی است.
- **معیار پذیرش:** قیمت‌ها از یک منبع قابل‌به‌روزرسانی بیایند و مدل ناشناخته
  به‌جای عدد غلط، صراحتاً «نامشخص» گزارش شود.

### N8 — تست سازگاری deserializer (بستن D1)
- **شدت:** 🟠 | **برآورد:** S | **وضعیت:** 🔴
- **معیار پذیرش:** تست دوجهته — thread قدیمی بدون `durationMs` بدون خطا خوانده
  شود، و ساختار جدید توسط منطق قدیمی‌تر بدون crash تحمل شود. همین الگو برای
  اسکیمای اپیزود/journal هم اجرا شود.

### N9 — بستن شکاف ریویو و CI روی PR #139 (بستن S4 و R2)
- **شدت:** 🟠 | **برآورد:** M | **وضعیت:** 🔴
- **مسئله:** PR ۷۴ فایل دارد ولی ریویوی دستی فقط روی زیرمجموعه‌ی ۳۴ فایلی بود
  (~۴۰ فایل بازبینی‌نشده)، و هیچ ورک‌فلوی بیلد/تستی روی آن اجرا نشده — فقط
  `label` و `size` که هر دو fail اند.
- **معیار پذیرش:** (الف) دیف فایل‌به‌فایلِ ~۴۰ فایل باقی‌مانده ریویو شود؛
  (ب) علت اجرا نشدن CI مشخص و رفع شود (یا صراحتاً به‌عنوان محدودیت ثبت شود)؛
  (ج) فیکس رِیس `875785e` که به شاخه‌ی PR پورت شد (`ddc1fab`) در همان ریویو دیده شود.

### N10 — دسترسی‌پذیری فوتر تزریق‌شده (بستن R6)
- **شدت:** 🟡 | **برآورد:** S | **وضعیت:** 🔴
- **معیار پذیرش:** دکمه‌های فوتر `aria-label` بگیرند و با کیبورد قابل رسیدن و
  فعال‌سازی باشند (هم در سورس، هم در پچ باندل).

## ۳) ترتیب پیشنهادی

1. **[Q7](07-source-build-campaign.md)** کمپین بیلد — تا نبندد، N1/N4/N8 و تست زنده‌ی
   Ledger ممکن نیستند
2. **N3** (قاعده‌ی وضعیت + همگام‌سازی) و **N2** — ارزان، اعتبار مستندات را برمی‌گرداند
3. **N1** + **N4** بلافاصله بعد از بیلد (گیت‌های واقعی)
4. **N8** و **N5** و **N6** — بهداشت، هرکدام S
5. **[Q9](09-tls-verification-disabled.md)** امنیتی — مستقل، هر وقت مالک وقت داشت
6. **N9** قبل از هر تلاش برای مرج #139
7. **C1** → **A1** → **Q3 فاز ۰** — کارِ بزرگِ بعدی
8. **N7** و **N10** — هر وقت
