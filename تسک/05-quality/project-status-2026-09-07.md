# وضعیت پروژه — ۲۰۲۶-۰۹-۰۷ (بعد از کمپین بیلد و نصب)

> به‌روزرسانی [رجیستر ۲۰۲۶-۰۹-۰۶](project-status-and-weaknesses-2026-09-06.md).
> همه‌ی اعداد از اجرای واقعی امروز؛ دستور بازتولید کنار هرکدام.

## ۱) در ۴۸ ساعت چه چیزی عوض شد

شش کامیت، ولی اثرشان از تعدادشان بزرگ‌تر است:

| کامیت | چه کرد |
|---|---|
| `875785e` | رفع رِیس `_activeLoopStartMs` (V4) |
| `f87636b` + `b5e612e` | تسک‌های Q7 (کمپین بیلد)، Q8 (رجیستر بدهی)، Q9 (TLS) |
| `3679ec6` | شش خطای تایپ واقعی + قابل‌اجرا کردن گیت hygiene روی این فورک |
| `bdac8e5` | **اولین کامپایل سبز تاریخ این فورک** |
| `8d4400d` | **اولین اجرای `test-node`**: ۱۱٬۹۱۳ تست |

و بیرون از گیت: بسته‌ی نصبی از سورس ساخته و **روی سیستم نصب شد**
(`1.99.3` → `1.127.0`، commit `8d4400da`).

## ۲) وضعیت تسک‌ها — عدد خام

از **۵۰ فایل تسک**: **۵ ✅ / ۱۲ 🟡 / ۳۳ 🔴**

> به‌روزرسانی ۲۰۲۶-۰۹-۰۸ (پس از هم‌ترازی مارکرها با کد — [گزارش ممیزی](../02-context/audit-2026-09-08.md)): عدد قبلی این سند «۴ ✅ / ۹ 🟡 / ۳۷ 🔴» بود و **دو جا کهنه شده بود**: Q7 با کامیت `00674fe` به ✅ رفت، و چهار تسک context (C1–C4) با شواهد کد از 🔴 به 🟡 اصلاح شدند.

```bash
for f in $(find "تسک" -name "*.md" -not -name "README.md" -not -name "00-*"); do \
  grep -m1 -o "وضعیت:\*\* *[^ |]*" "$f"; done | sort | uniq -c
```

✅ ها: M6 (سخت‌سازی Ledger)، Q4 (یکپارچگی live-patch)، Q5 (هارنس verify)، Q6 (قطع stream).

🟡 ها: M5 (Ledger)، C7 (compaction UX)، A6 (composer)، Q7 (کمپین بیلد)، Q8 (رجیستر)،
F3، F6، G0، G6.

**خواندن درست این عدد:** ۳۷ تای 🔴 کار «انجام‌نشده» است، ولی بخش بزرگی از انرژی دو روز
اخیر صرف چیزی شد که در هیچ‌کدام از آن ۵۰ فایل نبود — **قابل‌ساخت کردن خودِ پروژه**.
تا دیروز این کدبیس کامپایل نمی‌شد؛ حالا می‌شود، تست دارد، و نسخه‌ی نصب‌شده از خودش
ساخته شده.

## ۳) رجیستر ضعف‌ها — چه چیزی بسته شد

| ضعف | وضعیت | شاهد |
|---|---|---|
| **S1** هیچ بیلدی از سورس نیست | ✅ **بسته** | `COMPILE_EXIT=0`، صفر `error TS`، صفر `Debug Failure`، ۸۹۸۴ فایل در `out/` |
| **V1** ۶ فایل تست از ۸۷ اجرا می‌شد | ✅ **بسته** | `npm run test-node` → **۱۱٬۹۱۳ passing، ۰ failing** |
| **V4** رِیس duration | ✅ **بسته** | `875785e` |
| **R2** عدد غلط فایل‌های PR | ✅ **بسته** | سند تصحیح شد (۳۴ → ۷۴) |
| **R5** فایل‌های scratch تِرَک‌نشده | ✅ **بسته** | قاعده‌ی ignore اضافه شد |
| **P1–P4** بدهی live-patch | ⚪ **موضوعیت ندارد** | `--verify` → `4 OK, 6 PENDING, 89 MISSING`؛ پچ‌ها روی باندل ۱.۹۹.۳ بودند و آن باندل دیگر نصب نیست |
| **S3** Ledger هرگز زنده اجرا نشده | ✅ **بسته** (۲۰۲۶-۰۹-۰۸) | ledger زنده پیدا شد — ولی در ورک‌اسپیس **دیگری**: `escapezoom_dev/.inverse/ledger`. چهار thread، `lastSeq` = تعداد خطوط journal = بیشینه‌ی seq (۲/۴/۲/۲۹) و هر چهار تا gapless. آخرین نوشتن ۲۰۲۶-۰۹-۰۷ ۱۶:۱۸ |
| **S2** سه stack ایجنت | 🔴 باز | Q3 دست‌نخورده |
| **S4** CI روی PR #139 اجرا نشد | 🔴 باز | فقط `label` و `size`، هر دو fail |
| **V2** react src type-check ندارد | 🔴 باز | `src/tsconfig.json` فقط `./vs/**/*.ts` را می‌گیرد؛ `.tsx` اصلاً در برنامه نیست |
| **V3** CI مخصوص خودمان | 🔴 باز | هنوز فقط دستی |
| **D1/D2** سازگاری storage و مهاجرت IndexedDB | 🔴 باز | با اولین استفاده‌ی واقعی سنجیده می‌شود |
| **X1** جدول قیمت ۸ مدلی | 🔴 باز | — |
| **R1/R3/R4/R6** دریفت وضعیت، ignore متناقض، تسک فوتر، a11y | 🔴 باز | N1–N10 در [Q8](08-open-debt-register.md) |
| **Q9** TLS در سطح User | 🔴 باز | امنیتی، مستقل |

**تغییر مهم در ماهیت P1–P4:** کل جریان کاری live-patch — که ستون فقرات این پروژه بود —
با نصب بیلد از سورس **از کار افتاد**. این خبر خوبی است (دیگر جراحی روی باندل مینیفای
لازم نیست)، ولی یعنی `tools/live-patch.py` و ۹۷ پچش باید یا بازنشسته شوند یا برای
باندل جدید بازنویسی شوند. تصمیمش گرفته نشده.

## ۴) آنچه امروز حین نصب پیدا شد (هیچ‌کدام در تسک‌ها نبود)

| # | یافته | وضعیت |
|---|---|---|
| ۱ | `builtInExtensionsEnabledWithAutoUpdates` در product.json نبود → اسکنر اکستنشن‌ها در startup می‌مرد | رفع شد (کامیت نشده) |
| ۲ | `win32ContextMenu` نبود → بسته‌بندی ویندوز بعد از ۲۳ دقیقه می‌مرد | رفع شد با دو GUID تازه (کامیت نشده) |
| ۳ | محافظ `node-fetch` فقط به‌صورت live-patch وجود داشت و در سورس نیست | روی خروجی اعمال شد؛ **راه‌حل دائمی ندارد** |
| ۴ | `prepareBuiltInCopilotRipgrepShim` شکست می‌خورد (SDK کوپایلت در بسته نیست) | بی‌اثر بر خروجی، ولی بسته‌بندی را قرمز می‌کند |
| ۵ | دکمه‌ی Update نسخه را **عقب** می‌برد: تشخیص آپدیت بر اساس commit است و سرور، کامیت ما را نمی‌شناسد | موقتاً با `update.mode: none` |
| ۶ | لاگین گیت‌هاب: `gitHubClientId` **مال مایکروسافت** است (`01ab8ac9…`)، پس callback به `vscode://` می‌رود نه `neuralinverse://` | تصمیم مالک لازم است |
| ۷ | بیلد dev ثبت پروتکل `neuralinverse://` را از برنامه‌ی نصب‌شده ربوده بود | با نصب جدید برمی‌گردد |

**نکته‌ی الگو:** موارد ۱ و ۲ یک خانواده‌اند — کلیدی که کد بالادستی بدون گارد می‌خواند و
`product.json` این فورک اعلامش نکرده. احتمال دارد کلیدهای دیگری هم باشند که فقط در
مسیرهای اجرانشده ظاهر می‌شوند.

**بدهی حساب‌نشده:** `product.json` الان تغییر دارد و **قابل کامیت نیست**، چون
`build/hygiene.ts` هر product.json حاوی `extensionsGallery` را رد می‌کند و این فورک
gallery را به مارکت‌پلیس مایکروسافت وصل کرده. تا آن تصمیم گرفته نشود، دو فیکس بالا
فقط روی دیسک‌اند.

## ۵) ترتیب پیشنهادی از اینجا

1. **S3 را ببند** — یک چت واقعی در نسخه‌ی نصب‌شده، بعد بررسی `.inverse/ledger`.
   ارزان‌ترین گیت باقی‌مانده و بزرگ‌ترین ادعای اثبات‌نشده‌ی پروژه.
2. **تصمیم gallery** — چون تا حل نشود هیچ تغییر product.json کامیت نمی‌شود.
3. **لاگین گیت‌هاب** — OAuth App مال خودمان، یا اجبار به جریان device-code.
4. **`node-fetch` به‌صورت مرحله‌ی postinstall در سورس** — وگرنه هر `npm ci` باگ را برمی‌گرداند.
5. **سرنوشت live-patch** — بازنشستگی یا بازنویسی.
6. **V2/V3** — type-check سورس react و یک گیت خودکار.
7. بقیه‌ی [Q8](08-open-debt-register.md) و بعد C1 → A1 → Q3.
