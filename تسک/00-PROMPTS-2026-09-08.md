# 00 — پرامپت‌های اجرایی بعد از ممیزی ۲۰۲۶-۰۹-۰۸ (موج‌بندی موازی)

> مرجع: [ممیزی تسک‌ها در برابر کد](05-quality/task-vs-code-audit-2026-09-08.md) — کامیت `ebc8f379c8c`.
> برخلاف [`00-EXECUTION-PROMPT.md`](00-EXECUTION-PROMPT.md) که یک زنجیره‌ی سریالی است،
> این فایل **موج‌بندی‌شده** است: داخل هر موج می‌توانی همه را هم‌زمان به zCode بدهی.

## قاعده‌ی موازی‌سازی

موازی‌سازی امن است **فقط وقتی دو تسک فایل مشترک ندارند**. جدول زیر مبنای موج‌بندی است:

| موج | تسک | فایل‌های اصلی | تداخل؟ |
|---|---|---|---|
| ۱ | Q12 | `void/electron-main/llmMessage/sendLLMMessage.impl.ts` | — |
| ۱ | Q13 | `tools/verify.mjs`، `src/tsconfig.json`، `workbench.common.main.ts` | — |
| ۱ | A5-۱ | `void/browser/toolsService.ts` | — |
| ۱ | Q2 | حذف ۷ فایل `.bak` + یک `.js` مرده، `.gitignore` | — |
| ۲ | **A7** | `void/browser/voidModelProvider.ts` | بزرگ — تنها بماند |
| ۲ | M2 | `void/browser/agentMemoryService.ts`، `neuralInverseAgentService.ts` | — |
| ۲ | M1 | `neuralInverse/browser/workflowAgentService.ts` | — |
| ۳ | A3، A4، C2، E1 | بعد از A7 معنا پیدا می‌کنند | — |

**هشدار:** A7 و A5 هر دو مفهوم «فهرست ابزار مجاز» را لمس می‌کنند ولی در دو فایل جدا.
A5 را در موج ۱ فقط تا «enforce یا حذف» ببر؛ اعمالش در پل، کار A7 است.

---

## بخش الف — پرامپت شروع (یک بار، اول هر session زیcode)

```text
سلام. روی پروژه‌ی NeuralInverse کار می‌کنی (fork کد VS Code 1.127 / Void).
workspace: C:/Users/jobal/dev/neuralinverse

قبل از هر کاری:
1. `AGENTS.local.md` را کامل بخوان. همه‌ی دروازه‌هایش الزامی است — مخصوصاً:
   بدون تأیید صریح مالک هیچ push/issue/PR، و clean-room (کد هیچ پروژه‌ی
   لایسنس‌دار بیرونی عیناً کپی نمی‌شود).
2. `تسک/README.md` بخش «تعریف قطعی وضعیت» و «چهار شرط انجام‌شده» را بخوان.
3. `تسک/05-quality/task-vs-code-audit-2026-09-08.md` را بخوان — مخصوصاً بند ۸
   (داوری) و بند ۲ (کدی که کامپایل می‌شود ولی ثبت نمی‌شود).
4. `git status` و `git log --oneline -3` بزن و بگو روی چه شاخه‌ای هستی.

دو نکته‌ی محیطی که وقتت را هدر می‌دهند اگر ندانی:
- دستور `npm run typecheck-client` که در CLAUDE.md آمده **در این فورک وجود ندارد**.
  تایپ‌چک درست:
  NODE_OPTIONS="--max-old-space-size=8192" npx tsc -p src/tsconfig.json --noEmit
  با هیپ پیش‌فرض ۴GB به OOM می‌خورد (خروج ۱۳۴) و نتیجه‌ی گمراه‌کننده می‌دهد.
- «grep چیزی پیدا نکرد» شاهدِ نبودن نیست. سه تله‌ی این کدبیس: نام‌های snake_case،
  وصل‌شدن در لایه‌ای غیر از جایی که انتظار می‌رود، و import ترانزیتی از contrib دیگر.

قواعد کد: تب (نه فاصله)، بدون `any`، disposable ها بلافاصله بعد از ساخت register
شوند، رشته‌های کاربر با `nls.localize` خارجی‌سازی شوند، هدر کپی‌رایت روی فایل جدید.

حالا فقط گزارش بده و منتظر بمان. تسک را جداگانه می‌دهم.
```

---

# موج ۱ — چهار تسک، کاملاً موازی

> هر چهار را می‌توانی هم‌زمان به چهار session بدهی. هیچ فایل مشترکی ندارند.
> Q9 (امنیتی) کد نمی‌خواهد و کار خود مالک است — موازی با این‌ها انجام شود.

## پرامپت ۱-الف — Q12: `usage` روی مسیر desktop نمی‌رسد

```text
تسک: تسک/05-quality/12-desktop-usage-parity.md را بخوان و اجرا کن.

خلاصه‌ی مسئله: دو پیاده‌سازی موازی از sendLLMMessage داریم. نسخه‌ی common (وب)
شیء usage را به onFinalMessage می‌دهد، نسخه‌ی electron-main نمی‌دهد — و اپ نصب‌شده
همان electron-main را اجرا می‌کند.

اثبات کن اول:
  grep -n "onFinalMessage({" src/vs/workbench/contrib/void/common/llmMessage/sendLLMMessage.impl.ts
  grep -n "onFinalMessage({" src/vs/workbench/contrib/void/electron-main/llmMessage/sendLLMMessage.impl.ts
خطوط ۴۶۷ و ۶۷۰ در اولی usage دارند؛ ۴۶۸ و ۶۶۷ در دومی ندارند.

کار:
1. هر نقطه‌ی onFinalMessage در electron-main که معادل web اش usage می‌فرستد، usage
   بفرستد — با همان شکل و همان واحد. **قبل از تغییر، هر دو فایل را کامل مقایسه کن**
   و بگو آیا فیلد دیگری هم دریفت کرده یا فقط usage.
2. یک تست بنویس که **برابری قرارداد** دو مسیر را چک کند، نه فقط وجود usage در یکی.
   این تست باید هر دریفت آینده را بگیرد (آیتم N11 در رجیستر Q8).
3. تایپ‌چک با دستور ۸GB بالا.

گزارش بده: چند نقطه اصلاح شد، آیا فیلد دریفت‌کرده‌ی دیگری بود، و خروجی تست.
تغییر سورس را کامیت کن ولی push نکن. تست زنده‌ی روی نسخه‌ی نصبی کار مالک است.
```

## پرامپت ۱-ب — Q13: گیت ثبت contribution

```text
تسک: تسک/05-quality/13-contribution-registration-gate.md را بخوان و اجرا کن.

خلاصه: `neuralInverse.contribution.ts` هیچ importer ای ندارد (نه در src، نه build،
نه out) پس backgroundAgentService، composer، agentManagerPart و مارکت‌پلیس مدل
هرگز بارگذاری نمی‌شوند. ولی بخش‌های دیگر همان پوشه ترانزیتی زنده‌اند —
جدول تفکیک دقیق در فایل تسک است. **قبل از هر کاری آن جدول را با کد راستی‌آزمایی کن.**

کار (دو بخش، بخش ۲ مهم‌تر است):
1. تصمیم: گزینه‌ی ۲ فایل تسک را اعمال کن (کنارگذاشتن مستند مثل Q10) — یعنی کامنت
   صریح در workbench.common.main.ts که چرا ثبت نشده و شرط ثبت‌شدنش چیست.
   **گزینه‌ی «ثبتش کن» را انتخاب نکن** — تا A1 مغز نداشته باشد پنل خراب به کاربر
   نشان داده می‌شود.
2. گیت خودکار در tools/verify.mjs: هر *.contribution.ts که از هیچ ورودی workbench
   (مستقیم یا ترانزیتی) قابل رسیدن نیست build را قرمز کند، مگر در یک allowlist
   مستند با دلیل. روی وضعیت فعلی اجرا شود و دقیقاً موارد شناخته‌شده را گزارش کند
   (neuralInverse + Checks/Enclave در allowlist).

نکته: گیت باید import ترانزیتی را بفهمد، وگرنه false-positive می‌دهد. مثال واقعی
که باید **سبز** بماند: workflowAgentService از contrib/void/browser/toolsService.ts
خط ۳۴ زنده است.

گزارش بده: خروجی گیت روی وضعیت فعلی، و اینکه چند contribution یتیم پیدا شد.
```

## پرامپت ۱-ج — A5 بند اول: plan mode یا enforce یا حذف

```text
تسک: تسک/03-agent-parity/05-plan-mode.md را بخوان — فقط «اولین قدم».

مسئله‌ی ایمنی: ابزارهای plan_mode_enter/plan_mode_exit فلگ per-thread را set
می‌کنند ولی getThreadPlanMode صفر caller دارد:
  grep -rn "getThreadPlanMode" src --include=*.ts --include=*.tsx
فقط toolsService.ts:174 (تعریف) و :203 (پیاده‌سازی). یعنی کاربر Plan را روشن
می‌کند و هیچ write ای بلاک نمی‌شود. قابلیتی که ادعای مهار می‌کند و مهار نمی‌کند
از نبودنش بدتر است.

کار: یکی از دو راه را انتخاب کن و دلیلش را بنویس:
الف) enforce کن — در مرز اجرای ابزار در toolsService، وقتی plan mode روشن است هر
   ابزار نویسنده (فایل/ترمینال/git) رد شود با پیام روشن به مدل.
ب) حذف کن — هر دو ابزار و فلگ حذف شوند تا کسی به آن‌ها اتکا نکند.

توصیه: (الف) اگر می‌توانی مرز واحدی پیدا کنی که همه‌ی ابزارهای نویسنده از آن رد
شوند؛ اگر نه (ب)، چون enforcement نیمه از نبودش بدتر است.

مهم: دامنه را گسترش نده. اعمال plan در پل چت بومی (voidModelProvider) کار تسک A7
است و فایل دیگری دارد — دستش نزن.

تست: یک تست که ثابت کند در plan mode یک ابزار نویسنده رد می‌شود (اگر الف را
انتخاب کردی). تایپ‌چک با دستور ۸GB.
```

## پرامپت ۱-د — Q2: بهداشت کدبیس

```text
تسک: تسک/05-quality/02-codebase-hygiene.md را بخوان.

دامنه‌ی تأییدشده در ممیزی ۲۰۲۶-۰۹-۰۸ (بزرگ‌تر از متن اصلی تسک):
1. هفت فایل .bak که در git track شده‌اند:
   find src -name "*.bak*"
   (chatThreadService.ts.bak/.bak2/.bak3، powerModeTerminalHost.ts.bak/.bak2/.bak3،
    AgentNetworkViz.tsx.bak)
2. یک فایل JS مرده: src/vs/workbench/contrib/void/neuralInverse/browser/backgroundAgentService.js
   (۲۴۵ بایت، مسیر تودرتوی عجیب — قبل از حذف بگو از کجا آمده)
3. آیتم N5 رجیستر Q8: ۹ باندل React که هم‌زمان tracked و منطبق با الگوی ignore اند.
   این یکی **تصمیم سیاست می‌خواهد** — پیشنهاد بده، خودسرانه حذف نکن.

قبل از حذف هر فایل: مطمئن شو محتوایش جای دیگری نیست و چیزی import اش نمی‌کند.
یک الگوی ignore هم اضافه کن که *.bak* دوباره وارد نشود.

گزارش بده: چه حذف شد، فایل JS مرده از کجا آمده بود، و پیشنهادت برای باندل‌ها.
```

## Q9 — کار مالک، نه zCode (موازی با موج ۱)

`NODE_TLS_REJECT_UNAUTHORIZED=0` هنوز روی سطح User ست است و
`NODE_EXTRA_CA_CERTS` خالی. تأیید:

```powershell
[Environment]::GetEnvironmentVariable('NODE_TLS_REJECT_UNAUTHORIZED','User')
```

طبق [Q9](05-quality/09-tls-verification-disabled.md) رفع دوقدمی است: اول root CA
پروکسی/آنتی‌ویروس وارد trust store شود، **بعد** متغیر حذف شود — وگرنه هر ابزار
node می‌شکند و فشار برای برگرداندنش ایجاد می‌شود.

---

# موج ۲ — سه تسک موازی (بعد از سبزشدن موج ۱)

## پرامپت ۲-الف — A7: سخت‌سازی پل چت بومی ⭐ مهم‌ترین

```text
تسک: تسک/03-agent-parity/07-native-chat-bridge.md را کامل بخوان.

این پرلوریج‌ترین تسک بک‌لاگ است. خلاصه: voidModelProvider.ts مدل‌های BYOLLM ما را
به‌عنوان agent پیش‌فرض چت بومی VS Code 1.127 ثبت می‌کند (خط ۸۴۲ و ۸۶۷، فاز
BlockRestore). یعنی checkpoint فایلی، gauge مصرف، plan، مجوز، AGENTS.md، skills،
hooks و todo ی که هسته دارد، بالقوه با موتور ما کار می‌کنند. این تسک آن «بالقوه»
را «بالفعل» می‌کند.

پنج شکاف در فایل تسک آمده. **به این ترتیب برو** (هر بند مستقل کامیت شود):

1. بند «ب» اول — مرز واحد اجرای ابزار. الان خط ~۶۷۰ از _lmToolsService.invokeTool
   استفاده می‌کند ولی خط ~۵۷۲ یک مسیر نوشتن-فایل مستقیم دارد. هر ابزاری که مسیر
   دوم برود از confirmation و checkpoint هسته رد می‌شود. این را یکی کن.
   ← بدون این بند، بندهای A3 و A4 اصلاً کار نمی‌کنند.
2. بند «الف» — enforce کردن mode و فهرست ابزار انتخاب‌شده‌ی کاربر (خط ~۴۸۲).
   با خروجی تسک A5 هماهنگ باش.
3. بند «د» — فرستادن رویداد usage تا ویجت chatContextUsageWidget تغذیه شود.
   ← این بند به Q12 وابسته است؛ اگر Q12 سبز نشده صبر کن.
4. بند «ج» — وصل‌کردن ledger به VoidChatAgentImpl.
5. بند «ه» — یکدست‌کردن تبدیل پارامتر ابزار فایل.

قبل از شروع، بندهای ۱ و ۲ را با کد راستی‌آزمایی کن و اگر تحلیل تسک با کد نمی‌خواند
**اول به من بگو** — تحلیل روی کامیت ee636b38a52 نوشته شده.

معیارهای پذیرش شش‌گانه در فایل تسک است. برای هرکدام تست بنویس، مخصوصاً بند ۱
(«Plan واقعاً read-only است» — تست خودکار، نه بازرسی چشمی).

این تسک بزرگ است. اگر دیدی بیش از حد شد، بندهای ۱ و ۲ را کامل کن و گزارش بده.
```

## پرامپت ۲-ب — M2: وصل‌کردن recall به مسیر تزریق

```text
تسک: تسک/01-memory/02-vector-retrieval.md را بخوان — مخصوصاً کادر «اصلاح مارکر».

موتور hybrid کامل است و نیازی به نوشتن موتور دوم نیست. مسئله این است که روی مسیر
تزریق نیست:
  grep -rn "recallWithReasons" src --include=*.ts | grep -v agentMemoryService.ts
صفر نتیجه. مسیر تولیدی getContextSummary(1500) همگام و واژگانی را صدا می‌زند
(neuralInverseAgentService.ts:320).

کار (چهار بند فایل تسک):
1. جایگزینی getContextSummary با recallWithReasons در مسیر تزریق — با حفظ سقف
   توکن. توجه: recallWithReasons ناهمگام است و caller فعلی همگام؛ این را تمیز حل کن.
2. backfill بردار برای ورودی‌های قدیمی که embedding ندارند.
3. انتخاب معتبر provider (الان صرفاً از روی وجود شیء config تصمیم گرفته می‌شود).
4. نمایش «چرا این حافظه آمد» — دلیل تطبیق per-result از قبل در خروجی موتور هست.

اگر بند ۱ باعث شد مسیر همگام بشکند، **دامنه را کوچک کن و فقط بند ۱ را درست انجام
بده** — بقیه تسک جدا می‌شود. تایپ‌چک با دستور ۸GB.
```

## پرامپت ۲-ج — M1: ادامه‌ی نشست workflow

```text
تسک: تسک/01-memory/01-conversation-persistence.md را بخوان — کادر «اصلاح مارکر».

بخش ذخیره‌سازی حل شده (سه لایه: THREAD_STORAGE_KEY، ژورنال ledger، و
ChatSessionStore هسته). این تسک فقط بخش «ادامه‌ی نشست» است:

1. نگاشت agent→conversationId هنوز Map در RAM است
   (workflowAgentService.ts:143) و بعد از restart شناسه‌ی تازه می‌سازد → پایدارش کن.
2. دو run هم‌زمانِ یک agent همان شناسه را می‌گیرند → جداسازی run.
3. فقط turn موفق آرشیو می‌شود (خط ~۴۱۵) → تصمیم بگیر turn ناموفق چه شود و بنویس چرا.
4. فهرست/rename/delete/resume برای گفتگوهای workflow.

⚠️ workflowAgentService **زنده است** (ترانزیتی از contrib/void/browser/toolsService.ts
خط ۳۴) — برخلاف backgroundAgentService که ثبت نشده. پس این کار روی کد زنده است و
رگرسیون واقعی می‌سازد؛ تست بنویس.

اگر بند ۴ (UI) بزرگ شد، بندهای ۱-۳ را ببند و ۴ را جدا گزارش کن.
```

---

# موج ۳ — بعد از A7 (چهار تسک موازی)

بعد از سبزشدن بندهای ۱ و ۲ از A7، این چهار تسک از «ساختن» به «وصل‌کردن» تبدیل
می‌شوند و می‌توانند موازی بروند:

| تسک | دامنه‌ی جدید بعد از A7 |
|---|---|
| [A3](03-agent-parity/03-file-checkpoints.md) | انتخاب منبع حقیقت بین سرویس firmware و timeline بومی، بازنشستگی `agentRollbackService`، دکمه‌ی Restore |
| [A4](03-agent-parity/04-unified-permissions.md) | یکی‌کردن چهار مدل مجوز روی `languageModelToolsConfirmationService` هسته |
| [C2](02-context/02-context-gauge-ui.md) | فقط تغذیه‌ی ویجت آماده + adapter برای سایدبار |
| [E1](04-ecosystem/01-agents-md-compat.md) | افزودن `.neuralinverserules` به‌عنوان منبع پنجم کنار AGENTS.md/CLAUDE.md هسته |

**A1** (background agents) بعد از تصمیم Q13 و رفع باگ `/tmp` — و با دامنه‌ی جدید
«ثبت به‌عنوان session provider روی زیرساخت `agentSessions` upstream» نه ساختن پنل.

---

## چک‌لیست پایان هر تسک (به zCode بده)

```text
قبل از اینکه بگویی تمام شد:
1. NODE_OPTIONS="--max-old-space-size=8192" npx tsc -p src/tsconfig.json --noEmit
2. تست‌های مرتبط اجرا و سبز
3. چهار شرط «انجام شده» را جواب بده: فایل ✔ / ثبت ✔ / caller واقعی ✔ /
   سناریوی قابل‌مشاهده ✔ — اگر یکی جواب ندارد، تسک 🟡 است نه ✅
4. کامیت با پیام انگلیسی روشن. push نکن.
5. مارکر فایل تسک را طبق قاعده‌ی README به‌روز کن و بگو چه چیزی برای تست owner مانده.
```
